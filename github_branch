どのブランチいるかの確認
$ git branch

新規ローカルブランチを作る
$ git branch (作成するブランチ名)

作業ブランチの変更
$ git checkout (移動したい先のブランチ名)

branchをマスターにマージさせる
1.master branchに移動する
$ git checkout master
2.branchをmergeする
$ git merge (mergeしたいブランチ名) 
